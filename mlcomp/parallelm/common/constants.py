import logging

LOGGER_NAME_PREFIX = "parallelm"

LOG_LEVELS = {'noset': logging.NOTSET, 'debug': logging.DEBUG, 'info': logging.INFO, 'warn': logging.WARN,
              'error': logging.ERROR, 'critical': logging.CRITICAL}


REQUIREMENTS_FILENAME = "requirements.txt"