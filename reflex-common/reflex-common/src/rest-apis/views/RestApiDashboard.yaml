get:
  tags:
  - "reports"
  summary: "Extract dashboard view"
  produces:
  - "application/json"
  responses:
    200:
      description: "OK"
      schema:
        type: array
        items:
          $ref: './MCenterRestApi.yaml#/definitions/Dashboard'
    500:
      description: "Internal Server error"

